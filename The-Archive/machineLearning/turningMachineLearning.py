from random import randrange

limit = 100
piLimit = limit
poLimit = limit

class stateMachine:

class turingCell:

class memSpace:
    def __init__(self, neighbors, data=None):
        self.nebrs = neighbors
        self.data = data

class turingMind:
